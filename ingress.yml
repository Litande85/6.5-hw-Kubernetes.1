apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minimal-ingress
spec:
  rules:
  - http:
      paths:
      - pathType: Prefix
        path: /test
        backend:
          service:
            name: nginx-service
            port:
              number: 80